# Created Labels

Final submitted model:

```text
No additional labels were created or used.
```

Details:

- The final submission uses official challenge `train.csv` identity labels for supervised training.
- No pseudo-labels were used for `image_query` or `image_test`.
- No semantic class labels were used in the final ranking path.
- Target-camera style transfer uses only unlabeled RGB color statistics from official `image_query` images.

If later submitting a variant that uses generated labels, place the label files in a `labels/` folder, document their source here, and upload the same files to the Google Drive artifact folder required by the organizers.
